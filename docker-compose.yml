# Local development only — production is generated by GitHub Actions
# See .github/workflows/deploy.yml for the full production stack
services:
  app:
    build: .
    container_name: billingmate-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
